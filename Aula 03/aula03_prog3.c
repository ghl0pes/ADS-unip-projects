#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main() {
    int a, b, c, aux1,aux2,aux3;

    printf("Digite 3 numeros para ordena-los: ");
    scanf("%d %d %d", &a, &b, &c);

    if(b > a && c > a){
        aux1=a
        if (c > b) {
            aux2=b;
            aux3=c;
        } else {
        }
    } else {
        if(a > b) {
            aux = b;
        } else {

        }
    }

}
